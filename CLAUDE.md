# ハーネスとは？

AIエージェント(claude code)が参照する知識ファイルの総称。
オーナーの指示を遂行するために必要な知識が格納される。
ハーネスを自己改善するためのハーネスも含まれる。

* 内容による分類
    * メタハーネス
        * ハーネス自体の設計・更新・配置に関する１段メタな知識
    * ドメインハーネス
        * ドメイン領域の知識
* 導線による分類
    * 特殊ハーネス
        * AIエージェントが組み込みで特別な解釈をし、自動的な導線を持つ
        * 例：`CLAUDE.md`、rules、skills、hooks 等
    * 自然ハーネス
        * 任意のフォルダ構造、任意のファイルフォーマットを持つ
        * 課題やタスクに応じて最適な構造・フォーマットを設計する必要がある
        * 成果物を決定論的に検証するためのスクリプトや設定ファイルなど

詳細は `/harness-guide` スキルを参照。

# 作業原則

- 変更前に、目的・制約・完了条件・検証方法を言語化する。
- 小変更を除き、まず調査し、次に計画し、その後に実装する。
- 完了条件を決定論的に検証できるなら、検証を先に整備するか、少なくとも同時に整備する。
- 既存ファイルへ統合できる情報は、新規ファイルや追記型の新規章として積み上げない。
- 重複記述は禁止。参照で済むなら参照する。
- 変更後は、遠回りや詰まりの原因を見直し、再発防止に効く知識だけをハーネスへ反映する。

# タスク開始時の要求整理

新規タスクで目的・完了条件・制約・非目標・検証方法が曖昧な場合は、実装前に `/owner-contract` スキルを呼び出し、要求を構造化してから進める。単純な質問・小修正では呼び出さない。

# リサーチ方針

詳細は `/research-guide` スキルを参照。

# このリポジトリで避けること

- 一時メモを SSoT 化すること
- 変更履歴を本文へ積み上げること
- 検証不能な完了宣言
