# ハーネスとは？

AIエージェント(claude code)が参照する知識ファイルの総称。
オーナーの指示を遂行するために必要な知識が格納される。
ハーネスを改善するためのハーネスも含まれ、AIエージェントによるハーネスの自律改善を目指す。

* 内容による分類
    * メタハーネス
        * ハーネス自体の設計・更新・配置に関する１段メタな知識
    * ドメインハーネス
        * ドメイン領域の知識
* 導線による分類
    * 特殊ハーネス
        * AIエージェントが組み込みで特別な解釈をし、自動的な導線を持つ
        * 例：`CLAUDE.md`、rules、skills、hooks 等
    * 自然ハーネス
        * 任意のフォルダ構造、任意のファイルフォーマットを持つ
        * 課題やタスクに応じて最適な構造・フォーマットを設計する必要がある
        * 成果物を決定論的に検証するためのスクリプトや設定ファイルなど

**ハーネスファイルを編集・作成する前に必ず `/harness-guide` スキルを呼び出すこと。**

# 作業方針

オーナーの明示的な許可なく、AIエージェント自身の妄想・推測・提案をハーネスに書き込むことを禁止する。
ハーネスへの記述はオーナーが明示的に指示した内容のみとする。

大きめのスコープの指示（単純な質問・小修正ではない）をされ、かつ完了条件が明示されていない時、完了条件をオーナーに確認する

理由：完了条件を決定論的に検証できる場合、AIエージェントが自律して作業＋改善のループを回して完遂できるため。決定論的に処理できない場合でも成果物の品質向上を望める。

# ツール利用ルール

## playwright-cli

* オーナーが明示的に指示した場合や、ハーネスにplaywright-cliを使う旨の指示がある場合のみ使用できます
* ブラウザへの接続（playwright-cli attach）は禁止です。オーナーが予め実施しておきます
* 作業開始前に `playwright-cli list` で接続状態を確認してください
    * 未接続の場合はオーナーに接続を依頼して、処理を中断してください
* `.claude/skills/playwright-cli` は自動生成ファイルのため編集禁止です
* コマンド実行時は必ずセッション名を明示してください（例：`playwright-cli -s=chrome goto ...`）

# 改善提案後の返信解釈ルール

【改善提案】を提示した後にオーナーから返信が来た場合、以下のルールで解釈する。

* 「改善提案を修正して」「#1を直して」など、改善提案への修正指示だと明示的に読み取れる場合のみ、改善提案の修正として扱う
* それ以外の返信は、改善提案の直前の話題への返信として解釈する

# 文章執筆ルール

* 日本語の文章を書く場合、「。」の後は改行する
* ASCII Artで図を書くことは禁止

# その他

* あなたに指示を与える人のことをオーナーと呼びます